apiVersion: v1
kind: Secret
metadata:
  name: {{ include "test.fullname" . }}-kubernetes-dashboard-certs
  labels:
    k8s-app: kubernetes-dashboard
  {{- include "test.labels" . | nindent 4 }}
type: Opaque