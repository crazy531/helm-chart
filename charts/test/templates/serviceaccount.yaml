apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "test.fullname" . }}-kubernetes-dashboard
  labels:
    k8s-app: kubernetes-dashboard
  {{- include "test.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.kubernetesDashboard.serviceAccount.annotations | nindent 4 }}